// tests/user.test.js
/* globals describe, expect, test */

describe('Simple Jest Test', () => {
    test('should return true', () => {
        const value = true;
        expect(value).toBe(true);
    });
});
